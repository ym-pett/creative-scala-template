import doodle.core.*
import doodle.image.*
import doodle.image.syntax.all.*
import doodle.image.syntax.core.*
import doodle.java2d.*
import doodle.reactor.*
import scala.concurrent.duration.*
import cats.effect.unsafe.implicits.global


// @main def expandingExpressions(): Unit = {
//     (Image.triangle(10,5)):Image

    // Exercise: Abyssal Depths

    // val first_split = """
    // Be with me always—take any form—drive me mad! 
    // only do not leave me in this abyss, where I cannot find you!"
    // """.split("not ")

    // // learning how to access values in a list
    // val second_split = first_split(1).split(",")(0)

    // print(second_split)

    ///////

    // Exercise: Appending Strings

    // println("string_a" ++ "strin_b")
    // println("string_b".++ ("string_a"))

//  }
